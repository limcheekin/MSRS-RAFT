# RAFT Fine-tuning Requirements
# Python 3.10+

# Core deep learning
torch>=2.0.0
transformers>=4.38.0
accelerate>=0.27.0
peft>=0.8.0
bitsandbytes>=0.42.0

# Unsloth for efficient fine-tuning
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
unsloth_zoo

# TRL for SFT training
trl>=0.7.10

# Dataset handling
datasets>=2.16.0
huggingface_hub>=0.20.0

# Embeddings and retrieval
sentence-transformers>=2.3.0
faiss-cpu>=1.7.4  # Use faiss-gpu if you have GPU support
# faiss-gpu>=1.7.4  # Uncomment for GPU version

# Evaluation metrics
rouge-score>=0.1.2
bert-score>=0.3.13
nltk>=3.8.1
ragas>=0.1.0  # Optional, for advanced RAG metrics

# API clients
openai>=1.10.0  # For CoT generation with GPT-4

# Data processing
numpy==1.26.4
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0
python-dotenv>=1.0.0

# Logging and experiment tracking
tensorboard>=2.15.0
wandb>=0.16.0  # Optional, for experiment tracking

# Optional: Vector databases
# qdrant-client>=1.7.0  # If using Qdrant instead of FAISS

# Development tools (optional)
jupyter>=1.0.0
ipywidgets>=8.1.0
matplotlib>=3.7.0
seaborn>=0.13.0